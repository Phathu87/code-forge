# Local development defaults; real values belong in .env.local.
APP_ORIGIN=http://localhost:5173
PORT=3001
HOST=127.0.0.1
DATABASE_PATH=.data/codeforge.sqlite
# Production also requires NODE_ENV=production, HTTPS APP_ORIGIN and SMTP settings.
# SMTP_URL=smtps://username:password@mail.example.com:465
# MAIL_FROM=CodeForge <hello@example.com>
